# Telephony & LiveKit
livekit-agents>=0.12.0
livekit-plugins-elevenlabs>=0.8.0
livekit-api>=0.8.0

# AWS (moto for local mocking)
moto[server]>=5.0.0
boto3>=1.35.0
botocore>=1.35.0

# HTTP
httpx>=0.28.0
aiohttp>=3.11.0

# Utilities
pydantic-settings>=2.7.0
pydantic>=2.10.0
rich>=13.9.0

# Twilio (for SIP outbound if needed)
twilio>=9.4.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
